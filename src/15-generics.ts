/* function getValue(value: unknown) {
  return value;
}

getValue(12).toFixed(); */

function getValue<T>(value: T) {
  return value;
}

getValue(12).toFixed();
getValue('12').toString();
getValue([]).forEach;
